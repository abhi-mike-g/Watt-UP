package com.example.ondeviceagent

import android.content.Context

object AgentApi {
    // Simple API the app can implement to accept power modes
    // Example: send a broadcast with action "com.example.POWER_MODE" and extras {level: "low|med|high"}
}
